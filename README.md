# AnythingXpress — Native Node.js Rebuild

This repository contains the native Node.js/Express rebuild of the original AnythingXpress PHP/MySQL application. PHP is not required at runtime.

The full application source, deployment configuration, MySQL-compatible schema/migrations, public assets, and compatibility routes are being published here for Railway deployment.
